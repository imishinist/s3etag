[package]
name = "s3etag"
version = "0.1.0"
edition = "2021"

[lib]
name = "s3etag"
path = "src/lib.rs"

[dependencies]
clap = { version = "4.0", features = ["derive"], optional = true }
md5 = "0.8"

[features]
default = []
cli = ["clap"]
